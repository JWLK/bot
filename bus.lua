----버스 수신 기능 ----
function Busintro(user_id)
    send_text(user_id, bus_path .. 'busintro.txt', ok_cb, false)
end

function Busintro2(user_id)
    send_text(user_id, bus_path .. 'busintro2.txt', ok_cb, false)
end

function Bus0(user_id)
    send_text(user_id, bus_path .. 'bus0.txt', ok_cb, false)
end

function Bus1(user_id)
    send_text(user_id, bus_path .. 'bus1.txt', ok_cb, false)
end

function Bus2(user_id)
    send_text(user_id, bus_path .. 'bus2.txt', ok_cb, false)
end

function Bus3(user_id)
    send_text(user_id, bus_path .. 'bus3.txt', ok_cb, false)
end

function Bus4(user_id)
    send_text(user_id, bus_path .. 'bus4.txt', ok_cb, false)
end

function Bus5(user_id)
    send_text(user_id, bus_path .. 'bus5.txt', ok_cb, false)
end

function Bus6(user_id)
    send_text(user_id, bus_path .. 'bus6.txt', ok_cb, false)
end

function Bus7(user_id)
    send_text(user_id, bus_path .. 'bus7.txt', ok_cb, false)
end

function Bus8(user_id)
    send_text(user_id, bus_path .. 'bus8.txt', ok_cb, false)
end

function Bus9(user_id)
    send_text(user_id, bus_path .. 'bus9.txt', ok_cb, false)
end

function Bus10(user_id)
    send_text(user_id, bus_path .. 'bus10.txt', ok_cb, false)
end

function Bus11(user_id)
    send_text(user_id, bus_path .. 'bus11.txt', ok_cb, false)
end

function Bus12(user_id)
    send_text(user_id, bus_path .. 'bus12.txt', ok_cb, false)
end

function Bus13(user_id)
    send_text(user_id, bus_path .. 'bus13.txt', ok_cb, false)
end

function Bus14(user_id)
    send_text(user_id, bus_path .. 'bus14.txt', ok_cb, false)
end

function Bus15(user_id)
    send_text(user_id, bus_path .. 'bus15.txt', ok_cb, false)
end

function Bus16(user_id)
    send_text(user_id, bus_path .. 'bus16.txt', ok_cb, false)
end

function Bus17(user_id)
    send_text(user_id, bus_path .. 'bus17.txt', ok_cb, false)
end

function Bus18(user_id)
    send_text(user_id, bus_path .. 'bus18.txt', ok_cb, false)
end

function Bus19(user_id)
    send_text(user_id, bus_path .. 'bus19.txt', ok_cb, false)
end





----- 방학 중 비운행 노선 -----
function Busx1(user_id)
    send_msg(user_id, '동래는 방학중 버스를 운행하지 않습니다.', ok_cb, false)
end
 
function Busx2(user_id)
    send_msg(user_id, '개금은 방학중 버스를 운행하지 않습니다.', ok_cb, false)
end
 
function Busx3(user_id)
    send_msg(user_id, '해운대는 방학중 버스를 운행하지 않습니다.', ok_cb, false)
end

function Busx4(user_id)
    send_msg(user_id, '영부는 방학중 버스를 운행하지 않습니다.', ok_cb, false)
end

function Busx5(user_id)
    send_msg(user_id, '양산은 방학중 버스를 운행하지 않습니다.', ok_cb, false)
end

function Busx6(user_id)
    send_msg(user_id, '진해는 방학중 버스를 운행하지 않습니다.', ok_cb, false)
end

function Busx7(user_id)
    send_msg(user_id, '장유는 방학중 버스를 운행하지 않습니다.', ok_cb, false)
end

function Busx8(user_id)
    send_msg(user_id, '동래한전은 방학중 버스를 운행하지 않습니다.', ok_cb, false)
end
